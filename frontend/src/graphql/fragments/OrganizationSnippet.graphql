fragment OrganizationSnippet on Organization {
  id
  createdAt
  updatedAt
  typeOfOrganization
  name
  email
  phoneNumber
  address
  points
  voteStatus
  creatorId
  creator {
    id
    username
    email
  }
}
