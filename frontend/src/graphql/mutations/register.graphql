mutation Register(
  $username: String!
  $password: String!
  $confirmPassword: String!
) {
  register(
    options: {
      username: $username
      password: $password
      confirmPassword: $confirmPassword
    }
  ) {
    errors {
      field
      message
    }
    user {
      ...RegularUser
    }
  }
}
