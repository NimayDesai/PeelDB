query Organization($id: Int!) {
  organization(id: $id) {
    id
    createdAt
    updatedAt
    typeOfOrganization
    name
    email
    phoneNumber
    address
    points
    description
    voteStatus
    creatorId
    creator {
      id
      username
      email
    }
  }
}
