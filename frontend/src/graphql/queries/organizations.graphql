query Organizations(
  $limit: Int!
  $cursor: String
  $searchOptions: String!
  $searchValue: String!
) {
  organizations(
    limit: $limit
    cursor: $cursor
    searchOptions: $searchOptions
    searchValue: $searchValue
  ) {
    hasMore
    organizations {
      ...OrganizationSnippet
    }
  }
}
